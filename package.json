{
  "name": "microchat-docs",
  "version": "1.0.0",
  "description": "MicroChat policy and documentation pages",
  "type": "module",
  "scripts": {
    "build": "NODE_OPTIONS=--no-deprecation eleventy",
    "serve": "NODE_OPTIONS=--no-deprecation eleventy --serve --port 8080"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it-anchor": "^9.0.1"
  }
}
